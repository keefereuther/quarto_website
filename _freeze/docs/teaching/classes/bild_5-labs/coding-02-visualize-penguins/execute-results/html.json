{
  "hash": "e8b111e57874a791e4cab0b18493b9d2",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Visualize Penguins with ggplot2\"\nsubtitle: \"Build layered figures — histograms, scatterplots, and boxplots — all in your browser\"\nformat:\n  live-html:\n    theme:\n      light: [minty, ../../../../theme-light.scss]\n      dark: [quartz, ../../../../theme-dark.scss]\n    css: ../../../../styles.css\n    include-in-header: ../../../../_includes/skip-nav.html\n    page-layout: article\n    fontsize: 1.2em\n    toc: false\n    math: false\n    grid:\n      sidebar-width: 200px\n      body-width: 1000px\n      margin-width: 200px\n    webr:\n      packages:\n        - tidyverse\n        - palmerpenguins\n      cell-options:\n        min-lines: 4\n        caption: \"R code block\"\nengine: knitr\nexecute:\n  enabled: true\n  freeze: auto\nlive:\n  show-hints: true\n  show-solutions: true\n---\n\n::: {.cell}\n\n:::\n\n```{=html}\n<style>\n.card.exercise-editor {\n  --exercise-code-block-bg: #1e1e2e;\n  --exercise-code-block-color: #e6e7ee;\n  --exercise-main-color: #e6e7ee;\n  --exercise-cap-bg: #181825;\n  --exercise-gray: #3b3b52;\n  --exercise-primary-rgb: 137, 180, 250;\n  --exercise-line-bg: rgba(137, 180, 250, .12);\n  --exercise-line-gutter-bg: rgba(137, 180, 250, .22);\n  --exercise-editor-hl-kw: #ff9e64;\n  --exercise-editor-hl-cf: #ff9e64;\n  --exercise-editor-hl-st: #9ece6a;\n  --exercise-editor-hl-ss: #9ece6a;\n  --exercise-editor-hl-ch: #9ece6a;\n  --exercise-editor-hl-dv: #ffc777;\n  --exercise-editor-hl-bn: #ffc777;\n  --exercise-editor-hl-fl: #ffc777;\n  --exercise-editor-hl-cn: #ffc777;\n  --exercise-editor-hl-fu: #82aaff;\n  --exercise-editor-hl-va: #c0caf5;\n  --exercise-editor-hl-op: #89ddff;\n  --exercise-editor-hl-ot: #89ddff;\n  --exercise-editor-hl-at: #7dcfff;\n  --exercise-editor-hl-co: #9399b2;\n  --exercise-editor-hl-er: #f7768e;\n  border: 1px solid #3b3b52;\n  border-radius: 10px;\n  overflow: hidden;\n  box-shadow: 0 2px 10px rgba(0, 0, 0, .18);\n}\n.card.exercise-editor .card-header {\n  color: #e6e7ee;\n}\n.card.exercise-editor .card-header .btn.btn-outline-dark {\n  --bs-btn-color: #e6e7ee;\n  --bs-btn-border-color: #6c7086;\n  --bs-btn-hover-color: #1e1e2e;\n  --bs-btn-hover-bg: #e6e7ee;\n  --bs-btn-hover-border-color: #e6e7ee;\n  --bs-btn-active-color: #1e1e2e;\n  --bs-btn-active-bg: #e6e7ee;\n  --bs-btn-active-border-color: #e6e7ee;\n}\n</style>\n```\n\n\n\n\nWelcome back. Everything on this page runs real R in your browser — same setup as Coding Activity 1. You already plotted with base R's `hist()`; today you will **build figures in layers** with ggplot2.\n\nThe first time you click **Run Code**, your browser downloads R once (a few seconds). After that it is quick.\n\n::: {.callout-note}\n## The grammar of graphs\n\nEvery ggplot2 figure has three essential pieces:\n\n1. **data** — the dataset (for example `penguins`)\n2. **aes** — aesthetic mappings: which column goes on which axis, or which color\n3. **geom** — the geometric object: points, boxes, bars, histogram bins\n\nYou add layers with `+`. We will practice all three today.\n:::\n\n## 1. Load the tidyverse\n\nCoding Assignment 2 starts the same way. Run `library(tidyverse)` to load ggplot2 and friends. A conflicts message afterward is expected, not an error.\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='load_tidyverse'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: load_tidyverse\nlibrary()\n```\n:::\n\n\n\n\n::: {.hint exercise=\"load_tidyverse\"}\n::: {.callout-note collapse=\"false\"}\n## Hint\n\nFrom `?library`: `library(package, ...)` attaches a package so its functions become available. Replace `package` with the name of the collection you need (ggplot2 ships inside the tidyverse bundle).\n\nTry `?library` in R for the full help page.\n:::\n:::\n\n::: {.solution exercise=\"load_tidyverse\"}\n::: {.callout-tip collapse=\"false\"}\n## Solution\n\n```r\nlibrary(tidyverse)\n```\n\nOn DataHub and in your graded assignment you will type exactly this.\n:::\n:::\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='load_tidyverse' check='true'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: load_tidyverse\n#| check: true\ncode <- paste(.user_code, collapse = \"\\n\")\nif (!grepl(\"library\\\\s*\\\\(\\\\s*tidyverse\\\\s*\\\\)\", code)) {\n  list(\n    correct = FALSE,\n    type = \"error\",\n    message = \"Load the tidyverse with `library(tidyverse)`.\"\n  )\n} else {\n  list(\n    correct = TRUE,\n    type = \"success\",\n    message = \"Tidyverse loaded. ggplot2 is ready to use.\"\n  )\n}\n```\n:::\n\n\n\n\n## 2. Glimpse the penguins\n\n`glimpse()` is a tidyverse way to inspect a dataset: rows, columns, and column types. Run it on `penguins` from the `palmerpenguins` package so you know which columns to map to axes and colors.\n\n\n\n\n::: {.cell envir='visualize_penguins' setup='true' exercise='glimpse_penguins'}\n```{webr}\n#| envir: visualize_penguins\n#| setup: true\n#| exercise: glimpse_penguins\nlibrary(tidyverse)\nlibrary(palmerpenguins)\n```\n:::\n\n::: {.cell envir='visualize_penguins' exercise='glimpse_penguins'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: glimpse_penguins\n\n```\n:::\n\n\n\n\n::: {.hint exercise=\"glimpse_penguins\"}\n::: {.callout-note collapse=\"false\"}\n## Hint\n\nFrom `?glimpse`: `glimpse(x)` — print a compact summary of a data frame (rows, columns, types).\n\nExample with a built-in dataset:\n\n```r\nglimpse(mtcars)\n```\n\nApply the same function to the dataset named in the exercise prompt.\n:::\n:::\n\n::: {.solution exercise=\"glimpse_penguins\"}\n::: {.callout-tip collapse=\"false\"}\n## Solution\n\n```r\nglimpse(penguins)\n```\n\nYou should see 344 rows and 8 columns. `species` and `island` are `<fct>` (categories); measurements like `body_mass_g` are `<dbl>` (numbers).\n:::\n:::\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='glimpse_penguins' check='true'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: glimpse_penguins\n#| check: true\ncode <- paste(.user_code, collapse = \"\\n\")\nif (!grepl(\"glimpse\\\\s*\\\\(\", code)) {\n  list(\n    correct = FALSE,\n    type = \"error\",\n    message = \"Call `glimpse(penguins)` to inspect the dataset.\"\n  )\n} else if (!grepl(\"penguins\", code)) {\n  list(\n    correct = FALSE,\n    type = \"info\",\n    message = \"Pass `penguins` into `glimpse()`.\"\n  )\n} else {\n  list(\n    correct = TRUE,\n    type = \"success\",\n    message = \"344 rows, 8 columns — ready to map columns to a figure.\"\n  )\n}\n```\n:::\n\n\n\n\n::: {.callout-note collapse=\"true\"}\n## Check yourself\n\nFrom the `glimpse()` output:\n\n1. How many **rows** are there?\n2. What **type** is `body_mass_g`?\n3. Which column tells you **which island** each penguin was sampled on?\n\n**Answers:** 344 rows; `body_mass_g` is `<dbl>`; `island`.\n:::\n\n## 3. Histogram with ggplot2\n\nStart a figure with `ggplot()`, map `body_mass_g` to the x-axis, and add `geom_histogram()`. Use the `penguins` dataset directly — an upgrade from Coding Activity 1's `hist()`. Add a title with `labs(title = \"...\")`.\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='ggplot_histogram'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: ggplot_histogram\nggplot(penguins, aes(x = body_mass_g)) +\n  \n```\n:::\n\n\n\n\n::: {.hint exercise=\"ggplot_histogram\"}\n::: {.callout-note collapse=\"false\"}\n## Hint\n\nFrom `?ggplot`: `ggplot(data = NULL, mapping = aes(), ...)` — then add a layer with `+`.\n\nFrom `?geom_histogram`: bins one continuous variable.\n\nExample:\n\n```r\nggplot(mtcars, aes(x = mpg)) +\n  geom_histogram() +\n  labs(title = \"Car fuel economy\")\n```\n\nMap the x-axis to one continuous column from `penguins` and add your own title.\n:::\n:::\n\n::: {.solution exercise=\"ggplot_histogram\"}\n::: {.callout-tip collapse=\"false\"}\n## Solution\n\n```r\nggplot(penguins, aes(x = body_mass_g)) +\n  geom_histogram() +\n  labs(title = \"Penguin body mass\")\n```\n\nA histogram shows how **one continuous variable** is distributed — how many penguins fall in each weight range. ggplot2 may warn about two rows with missing body mass; that is normal.\n:::\n:::\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='ggplot_histogram' check='true'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: ggplot_histogram\n#| check: true\ncode <- paste(.user_code, collapse = \"\\n\")\nhas_ggplot <- grepl(\"ggplot\\\\s*\\\\(\", code)\nhas_hist <- grepl(\"geom_histogram\\\\s*\\\\(\", code)\nhas_labs <- grepl(\"labs\\\\s*\\\\(\", code)\nif (has_ggplot && has_hist && has_labs) {\n  list(\n    correct = TRUE,\n    type = \"success\",\n    message = \"Histogram with a title — distribution of one continuous variable.\"\n  )\n} else if (has_ggplot && has_hist) {\n  list(\n    correct = FALSE,\n    type = \"info\",\n    message = \"Add `labs(title = \\\"...\\\")` with your own title.\"\n  )\n} else {\n  list(\n    correct = FALSE,\n    type = \"error\",\n    message = \"Use `ggplot(...) + geom_histogram() + labs(title = \\\"...\\\")`.\"\n  )\n}\n```\n:::\n\n\n\n\n## 4. Scatterplot — two continuous variables\n\nBuild a **scatterplot**: flipper length on the x-axis, body mass on the y-axis. Map `species` to **color** so each species gets its own hue. Use `geom_point()`.\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='ggplot_scatter'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: ggplot_scatter\nggplot(penguins, aes(\n  x = flipper_length_mm,\n  y = body_mass_g,\n  color = species\n)) +\n  \n```\n:::\n\n\n\n\n::: {.hint exercise=\"ggplot_scatter\"}\n::: {.callout-note collapse=\"false\"}\n## Hint\n\nFrom `?geom_point`: scatterplot of two continuous variables. Map a third variable to `color` inside `aes()`.\n\nExample:\n\n```r\nggplot(mtcars, aes(x = wt, y = mpg, color = factor(cyl))) +\n  geom_point()\n```\n\nUse flipper length and body mass on the axes and map species to color on the penguin data.\n:::\n:::\n\n::: {.solution exercise=\"ggplot_scatter\"}\n::: {.callout-tip collapse=\"false\"}\n## Solution\n\n```r\nggplot(penguins, aes(\n  x = flipper_length_mm,\n  y = body_mass_g,\n  color = species\n)) +\n  geom_point()\n```\n\nScatterplots relate **two continuous measurements**. Color adds a **third variable** — here, species — so you can see clusters.\n:::\n:::\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='ggplot_scatter' check='true'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: ggplot_scatter\n#| check: true\ncode <- paste(.user_code, collapse = \"\\n\")\nif (grepl(\"ggplot\\\\s*\\\\(\", code) && grepl(\"geom_point\\\\s*\\\\(\", code) && grepl(\"color\\\\s*=\\\\s*species\", code)) {\n  list(\n    correct = TRUE,\n    type = \"success\",\n    message = \"Scatterplot with species as color — two continuous vars plus a grouping.\"\n  )\n} else if (grepl(\"geom_point\\\\s*\\\\(\", code)) {\n  list(\n    correct = FALSE,\n    type = \"info\",\n    message = \"Map `color = species` inside `aes()`.\"\n  )\n} else {\n  list(\n    correct = FALSE,\n    type = \"error\",\n    message = \"Use `geom_point()` with flipper length, body mass, and `color = species`.\"\n  )\n}\n```\n:::\n\n\n\n\n## 5. Boxplot — compare groups\n\nCoding Assignment 2 builds a boxplot **one layer at a time**. Here is the core pattern:\n\n- Put **island** on the x-axis, **body_mass_g** on the y-axis\n- Add `geom_boxplot()` with `fill` mapped to island\n- Add `theme_minimal()` and readable `labs()`\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='ggplot_boxplot'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: ggplot_boxplot\nggplot(penguins, aes(\n  x = island,\n  y = body_mass_g,\n  fill = island\n)) +\n  \n```\n:::\n\n\n\n\n::: {.hint exercise=\"ggplot_boxplot\"}\n::: {.callout-note collapse=\"false\"}\n## Hint\n\nFrom `?geom_boxplot`: compare a numeric variable across groups.\n\nExample pattern:\n\n```r\nggplot(mtcars, aes(\n  x = factor(cyl),\n  y = mpg,\n  fill = factor(cyl)\n)) +\n  geom_boxplot() +\n  theme_minimal() +\n  labs(title = \"MPG by cylinder count\", x = \"Cylinders\", y = \"MPG\")\n```\n\nApply the same layering — boxplot, fill, `theme_minimal()`, `labs()` — to islands and body mass.\n:::\n:::\n\n::: {.solution exercise=\"ggplot_boxplot\"}\n::: {.callout-tip collapse=\"false\"}\n## Solution\n\n```r\nggplot(penguins, aes(\n  x = island,\n  y = body_mass_g,\n  fill = island\n)) +\n  geom_boxplot() +\n  theme_minimal() +\n  labs(\n    title = \"Body mass by island\",\n    x = \"Island\",\n    y = \"Body mass (g)\"\n  )\n```\n\nBoxplots compare a **continuous variable** across **groups**. On your graded assignment you will add `reorder()` to sort groups by elevation after you join that metadata in Coding Activity 3.\n:::\n:::\n\n\n\n\n::: {.cell envir='visualize_penguins' exercise='ggplot_boxplot' check='true'}\n```{webr}\n#| envir: visualize_penguins\n#| exercise: ggplot_boxplot\n#| check: true\ncode <- paste(.user_code, collapse = \"\\n\")\nhas_box <- grepl(\"geom_boxplot\\\\s*\\\\(\", code)\nhas_theme <- grepl(\"theme_minimal\\\\s*\\\\(\", code)\nhas_labs <- grepl(\"labs\\\\s*\\\\(\", code)\nif (has_box && has_theme && has_labs) {\n  list(\n    correct = TRUE,\n    type = \"success\",\n    message = \"Layered boxplot with fill, theme, and labels — Assignment 2 ready.\"\n  )\n} else if (has_box) {\n  list(\n    correct = FALSE,\n    type = \"info\",\n    message = \"Add `theme_minimal()` and `labs(title = ..., x = ..., y = ...)`.\"\n  )\n} else {\n  list(\n    correct = FALSE,\n    type = \"error\",\n    message = \"Include `geom_boxplot()`, `theme_minimal()`, and `labs()`.\"\n  )\n}\n```\n:::\n\n\n\n\n## 6. Recap — ggplot verbs you practiced\n\n::: {.callout-note collapse=\"true\"}\n## What does each function do?\n\n| Function | Role |\n|----------|------|\n| `library(tidyverse)` | Loads ggplot2 and related packages |\n| `glimpse()` | Quick look at rows, columns, and types |\n| `ggplot()` + `aes()` | Starts a figure and maps columns to axes, color, or fill |\n| `geom_histogram()` | Distribution of one continuous variable |\n| `geom_point()` | Relationship between two continuous variables |\n| `geom_boxplot()` | Compare a continuous variable across groups |\n| `theme_minimal()` + `labs()` | Cleans appearance and adds titles/labels |\n\nIf you can explain each row in your own words, you have covered the figure-building core of **Coding Assignment 2**.\n:::\n\n## 7. Transfer to Coding Assignment 2\n\nOn Canvas you will build figures step by step with **hemoglobin** measurements and **population elevation** data:\n\n1. `library(tidyverse)` and read your dataset\n2. Build a boxplot one layer at a time with `ggplot()`, `geom_boxplot()`, `fill`, `theme_minimal()`, and `labs()`\n\nYou practiced the layering here. Coding Activity 3 covers the wrangling steps — filtering, reshaping, and joining — that prepare the table for those plots.\n\n## Keep playing\n\nNo grading here. Try swapping geoms, mapping a different column to color, or predicting what a plot will look like before you run it.\n\n\n\n\n::: {.cell envir='visualize_penguins'}\n```{webr}\n#| envir: visualize_penguins\nggplot(penguins, aes(x = species, y = bill_length_mm, fill = species)) +\n  geom_boxplot(show.legend = FALSE) +\n  theme_minimal() +\n  labs(title = \"Bill length by species\", x = NULL, y = \"Bill length (mm)\")\n```\n:::\n\n\n\n\n::: {.callout-tip appearance=\"simple\"}\n**Try this with a partner.** One of you predicts the figure; the other runs the code. Swap roles. Saying your prediction out loud first is one of the fastest ways to learn.\n:::\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}